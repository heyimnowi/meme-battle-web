import { createTheme  } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#ffffff",
    },
    secondary: {
      main: "#FFD600",
    },
    text: {
      primary: "#000000",
      secondary: "#ffffff",
    },
  },
});
